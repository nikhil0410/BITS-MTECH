# Python virtual environment (machine-specific — recreate with pip)
.venv/
venv/

# Python caches
__pycache__/
*.py[cod]
*.egg-info/

# OS cruft
.DS_Store

# Generated outputs — all regenerated by running the pipelines.
# NOTE: outputs/mlflow.db and outputs/mlartifacts store absolute paths, so they
# must NOT be moved between machines; re-run ml_pipeline.py to repopulate them.
# outputs/
